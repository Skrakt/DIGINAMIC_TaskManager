{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --prefix app && npm ci --prefix lib && npm run build --prefix app && npm run build --prefix lib"
  },
  "deploy": {
    "startCommand": "npm run start:prod --prefix lib",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
