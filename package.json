{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "name": "task-manager",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run start --prefix app\" \"npm run start:dev --prefix lib\"",
    "lint": "npm run lint --prefix app && npm run lint --prefix lib",
    "build": "npm run build --prefix app && npm run build --prefix lib"
  }

}
